"""
live_monitor —— 实时市场监控与信号看板子系统（纸上模拟/研究用途）

与 backtest/ 完全隔离的独立子系统：backtest/ 是"本地静态历史数据批量
回测"，live_monitor/ 是"实时行情监控 + 信号留痕 + 大屏展示"。两者不
共享数据库（backtest 用 peewee/SQLite，live_monitor 用 MySQL/Redis），
不共享调用路径，互不影响。

**范围声明**：本子系统不接任何真实交易所下单接口，只做信号的计算、
留痕、广播、展示，属于纸上模拟/研究工具。如果未来要接真实下单，需要
另外评估风控、资金上限、异常熔断等安全机制，不在本子系统范围内。
"""
